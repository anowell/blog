<!DOCTYPE html>
<html lang="en">
  <head>
    {% include 'head.liquid' %}
  </head>
  <body>
    <div class="shell">
      {% assign nav = page.data.nav %}
      {% include 'sidenav.liquid' %}
      <main class="main {{ page.data.page_class }}">
        {{ page.content }}
      </main>
    </div>
    {% include 'foot.liquid' %}
  </body>
</html>
